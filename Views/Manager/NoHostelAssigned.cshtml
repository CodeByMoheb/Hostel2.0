@{
    ViewData["Title"] = "No Hostel Assigned";
}

<div class="container-fluid">
    <div class="text-center">
        <div class="error mx-auto" data-text="404">
            <i class="fas fa-exclamation-triangle fa-5x text-warning mb-4"></i>
        </div>
        <p class="lead text-gray-800 mb-5">You are not assigned to manage any hostel</p>
        <p class="text-gray-500 mb-0">Please contact the system administrator to get assigned to a hostel.</p>
        <hr>
        <a asp-controller="Home" asp-action="Index" class="btn btn-primary">
            <i class="fas fa-home"></i> Return to Home
        </a>
    </div>
</div> 